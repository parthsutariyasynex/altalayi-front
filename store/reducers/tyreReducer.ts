const initialState = {
    widths: [],
    heights: [],
    rims: [],
    loading: false,
    error: null,
};

const tyreReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default tyreReducer;
